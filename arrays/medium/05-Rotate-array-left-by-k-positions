//  Rotate array left by k positions
let arr = [1, 2, 3, 4, 5]
let k = 2
// Output: 3, 4, 5, 1, 2]
k = k % arr.length
let result = arr.slice(k, arr.length).concat(arr.slice(0, k))
console.log(result)
// Time Complexity ---> O(n)
// Space Complexity ---> O(n)
